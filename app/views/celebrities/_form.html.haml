= form_for @celebrity do |f|
  - if @celebrity.errors.any?
    #error_explanation
      %h2= "#{pluralize(@celebrity.errors.count, "error")} prohibited this celebrity from being saved:"
      %ul
        - @celebrity.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :handle
    = f.text_field :handle
  .actions
    = f.submit 'Save'
