= form_for [@celebrity, @tweet] do |f|
  - if @tweet.errors.any?
    #error_explanation
      %h2= "#{pluralize(@tweet.errors.count, "error")} prohibited this tweet from being saved:"
      %ul
        - @tweet.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :tweet
    = f.text_field :tweet
  .field
    = f.label :answer
    = f.text_field :answer
  .actions
    = f.submit 'Save'
